dependencies {
    def supportVer = project.hasProperty("supportVersion") ? supportVersion : "+";
    compile 'com.theartofdev.edmodo:android-image-cropper:2.7.0@aar'
    compile "com.android.support:exifinterface:$supportVer"
}
